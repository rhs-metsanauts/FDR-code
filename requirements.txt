# Mission Control (laptop)
flask>=3.0
requests>=2.31
ollama>=0.2
websocket-client>=1.7
pydantic>=2.0

# Rover FastAPI server (Jetson / Pi with FastAPI)
fastapi>=0.110
uvicorn>=0.29

# Rover hardware (Raspberry Pi only)
RPi.GPIO>=0.7
pi-servo-hat>=0.0.4
adafruit-circuitpython-rfm9x>=3.4

# Serial comms
pyserial>=3.5

# Mapping / ZED (Jetson only)
# pyzed is installed via the ZED SDK installer, not pip
numpy>=1.24
websockets>=12.0

# Camera
opencv-python>=4.9
